<%= form_with(model: aaaa, local: true) do |form| %>
  <% if aaaa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(aaaa.errors.count, "error") %> prohibited this aaaa from being saved:</h2>

      <ul>
      <% aaaa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :aaaa_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
